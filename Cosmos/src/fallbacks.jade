script(description='fallback for React').
  try{
    function addToDom(){
      console.log('failed to reach cdn React');
      document.write('<script src="frameworks/react.js"><\/script>');
    }
    window.React ? window.React : addToDom()
  }catch(e){
    console.log('failed to reach cdn React');
  }
script(description='fallback for ReactDOM').
  try{
    function addToDom(){
      console.log('failed to reach cdn ReactDOM');
      document.write('<script src="frameworks/react-dom.js"><\/script>');
    }
    window.ReactDOM ? window.ReactDOM : addToDom()
  }catch(e){
    console.log('failed to reach cdn ReactDOM');

  }
script(description='fallback for ReactRouter').
  try{
    function addToDom(){
      console.log('failed to reach cdn ReactRouter');
      document.write('<script src="frameworks/ReactRouter.js"><\/script>');
    }
    window.ReactRouter ? window.ReactRouter : addToDom()
  }catch(e){
    console.log('failed to reach cdn ReactRouter');

  }  
script(description='fallback for TweenMax').
  try{
    function addToDom(){
      console.log('failed to reach cdn TweenMax');
      document.write('<script src="frameworks/TweenMax.min.js"><\/script>');
    }
    window.TweenMax ? window.TweenMax : addToDom()
  }catch(e){
    console.log('failed to reach cdn TweenMax');
  } 
