import applyBackground from './apply-background';
import applyTypeColor from './apply-type-color';
import resizeDisplay from './resize-display';
import resizePannel from './resize-Pannel';
import enableConsoleless from './enable-disable';
import enableAnimations from './enable-animations';
import settings from './settings';
import setDisplay from './set-display';

export let userInteraction = {
  applyBackground:applyBackground,
  applyTypeColor:applyTypeColor,
  resizeDisplay:resizeDisplay,
  resizePannel:resizePannel,
  settings:settings,
  enableConsoleless:enableConsoleless,
  enableAnimations:enableAnimations,
  setDisplay:setDisplay
}
